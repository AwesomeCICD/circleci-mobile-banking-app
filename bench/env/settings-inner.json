{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "INNER arm — the ONLY validation is the chunk-sidecar Stop hook (runs the full gate set on the working tree after every turn). The demo's local PreToolUse bundle hook is intentionally removed here so the inner arm's validation cost is the sidecar alone, not sidecar + local bundle.",
  "permissions": {
    "allow": [
      "Bash(chunk:*)",
      "Bash(git add:*)",
      "Bash(git commit:*)",
      "Bash(git push:*)",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)"
    ]
  },
  "hooks": {
    "Stop": [
      {
        "hooks": [
          { "type": "command", "command": "chunk validate", "timeout": 600 }
        ]
      }
    ]
  }
}
